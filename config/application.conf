# Configuration settings for the Keycloak Nextcloud Provisioner

# Keycloak configuration
keycloak {
    auth-server-url = "http://localhost:8080/auth"
    realm = "your-realm"
    client-id = "your-client-id"
    client-secret = "your-client-secret"
}

# Logging configuration
logging {
    level = "INFO"
    file = "logs/application.log"
}

# Provisioning settings
provisioning {
    enable-user-provisioning = true
    enable-tenant-provisioning = true
}

# (Placeholder) Configuration file for keycloak-kafka-bridge
# Currently runtime configuration is supplied through Keycloak SPI parameters.
# Add additional configuration sections here if externalized config becomes necessary.